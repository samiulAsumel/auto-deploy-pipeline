# ════════════════════════════════════════════════════════════════════════════
# Automated Deployment Pipeline — Development Overrides  v1.0.0
# Sourced AFTER config.conf when ENVIRONMENT=dev.
# ════════════════════════════════════════════════════════════════════════════

BRANCH="develop"
RUN_TESTS=false
BUILD_ENABLED=false
AUTO_ROLLBACK=false
HEALTH_URL="http://localhost:3000/health"
NOTIFY_ON="failure"
KEEP_RELEASES=2
