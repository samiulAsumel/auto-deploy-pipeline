# ════════════════════════════════════════════════════════════════════════════
# Automated Deployment Pipeline — Production Overrides  v1.0.0
# Sourced AFTER config.conf when ENVIRONMENT=production.
# Only add settings that differ from config.conf defaults.
# ════════════════════════════════════════════════════════════════════════════

BRANCH="main"
RUN_TESTS=true
BUILD_ENABLED=true
AUTO_ROLLBACK=true
HEALTH_URL="https://app.company.com/health"
NOTIFY_ON="both"
KEEP_RELEASES=5
